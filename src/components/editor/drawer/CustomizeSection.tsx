import { IComponent } from "@/types/schema";
import React from "react";

const CustomizeSection = ({ data }: { data?: IComponent }) => {
  return <div>CustomizeSection</div>;
};

export default CustomizeSection;
